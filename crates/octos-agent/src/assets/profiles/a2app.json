{
  "name": "a2app",
  "version": 1,
  "description": "Agent-to-App (a2app) profile — builds and deploys native Makepad/Splash UIs. Uses the fake Splash counter provider for testing the Makepad channel adapter and pipeline deployment flow.",
  "tools": {
    "mode": "allow_list",
    "tools": [
      "group:fs",
      "group:runtime",
      "launch_app",
      "message",
      "spawn"
    ]
  },
  "mcp_servers": [],
  "permissions": "default",
  "agents": ["research-worker", "repo-editor"]
}
